/**
 * Created by 14486 on 2019/3/16.
 */
// 判断
// 获取的颜色或者路径最好不要用来做判断 格式可能不一样
let x = 20;
if( x>20){ // 条件可以是一个单独的数据 也可以是一个语句 <= < > >= != !== == ===
    // 执行代码
}else if( x=20 ){

}else{

}
let name = "罗恒";
switch (name) { // 全等判断
    case "范雨菲":
        console.log(1);
        break;
    case "乌拉":
        console.log("乌拉");
        break;
    default:
        console.log("默认");
        break;
}
// 图片显示隐藏 1:改变src 不推荐  2:图片显示隐藏

